CREATE TABLE %s (
	id BIGINT(20) NOT NULL AUTO_INCREMENT,
	attribute_postid  bigint(20) NOT NULL,
	attribute_userid VARCHAR(100) NOT NULL,
        attribute_date TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
        attribute_sub_subscribe int NOT NULL DEFAULT 1 ,
	PRIMARY KEY  (id)
);