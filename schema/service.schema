CREATE TABLE %s (
        id BIGINT(20) NOT NULL AUTO_INCREMENT,
	service_name VARCHAR(100) NOT NULL,
	service_label VARCHAR(100),
	attribute_source_as VARCHAR(100) NOT NULL,
	attribute_orderby VARCHAR(100) NOT NULL,
	PRIMARY KEY  (id)
);